---
last_updated: 2026-09-22T07:20:00Z
status: active
---

# Project Context

## Project Overview

Atoms 风格 AI 应用生成平台的官网 + 产品演示页。用户用一句自然语言描述需求，页面展示「需求输入 → 六阶段生成流水线 → 可运行应用预览」的完整链路。阶段一交付纯前端演示版；阶段二已接入 Atoms Cloud：账号复用平台认证，项目/任务/版本/配额持久化，首页生成链路改为创建任务并轮询服务端阶段状态，登录用户可查看自己的项目列表。

## Key Decisions
| Date | Decision | By | Rationale |
|------|----------|-----|-----------|
| 2026-09-22 | 阶段一不接入后端，生成流程用本地定时状态机表达 | Alex | 演示期只需表达产品逻辑，避免无需求的后端复杂度 |
| 2026-09-22 | 预览窗用真实 React 组件渲染，而非截图 | Alex | 与 mission 的「不生成点不动的假界面」一致，也更可信 |
| 2026-09-22 | 模板缩略图用 CSS 线框 | Alex | 避免无意义 AI 配图，保持工程风一致 |
| 2026-09-22 | 流水线由五步对齐为六步（补入测试验证） | Alex | 与 `docs/plan.md` 的状态模型及 mission 原则 2 对齐 |
| 2026-09-22 | 未落地模块统一使用「建设中」占位页，不伪造成已完成 | Alex | 落实 mission 原则 1 与原则 2 |
| 2026-09-22 | 阶段二前端改动面：生成链路、账号状态、项目列表与详情页、导航新增「我的项目」 | Alex | 首页既有区块与布局结构未改动，降低回归面 |
| 2026-09-22 | 账号统一走 `client.auth.*`，不启用仓库中旧的 Axios 认证工具 | Alex | 避免出现第二套认证体系 |
| 2026-09-22 | 阶段推进由服务端按任务创建时间推导，前端只轮询 | Alex | 过程可查询、刷新不丢失，且无需引入队列 |
| 2026-09-22 | 失败停在失败阶段，重试开启新一批任务且不重复扣额度 | Alex | 落实「失败不清空」，同时避免配额争议 |
| 2026-09-22 | 阶段二 `preview_url` 留空，不伪造可访问地址 | Alex | 真实部署地址属于阶段三，不制造假成功态 |
| 2026-09-22 | 「免费开始」用 sessionStorage 意图 + 全局兜底消费实现登录后回跳聚焦 | Alex | 登录落点由平台 `/auth/callback` 控制，应用无法指定回跳地址 |

## Constraints

- 视觉规范以 `app/frontend/DESIGN.md` 为准：暗色为唯一主题、主色 `#C8F751` 占比 ≤10%、不使用蓝紫渐变。
- 更新日志为平台自身的发布记录（`/changelog`），与用户生成项目的变更追溯是两个不同对象，后者未落地。
- 产品边界以 `docs/mission.md` 为准：不做通用代码补全、不做拖拽搭建器、不做假界面。
- 前端固定为 shadcn/ui + Vite 技术栈；后端后续使用 Atoms Cloud，不自建第二套认证。
- 阶段二实际改动面为：生成链路（`run()` + 轮询）、账号状态、项目列表与详情页、导航新增「我的项目」；首页既有区块与布局结构未被改动。
- 构建策略：开发任务收尾时运行 `pnpm run lint && pnpm run build` 作为验证；纯文档或问答轮次不做构建。
- 交付策略：收到明确的开发或文档更新指令后动手；未获指令时只回答与分析，不主动重构。
