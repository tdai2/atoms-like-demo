---
last_updated: 2026-09-23T12:00:00Z
status: active
---

# Project Context

## Project Overview

Atoms 风格 AI 应用生成平台的官网 + 产品演示页。用户用一句自然语言描述需求，页面展示「需求输入 → 六阶段生成流水线 → 可运行应用预览」的完整链路。阶段一交付纯前端演示版；阶段二已接入 Atoms Cloud：账号复用平台认证，项目/任务/版本/配额持久化，首页生成链路改为创建任务并轮询服务端阶段状态，登录用户可查看自己的项目列表。阶段三已把生成链路替换为真实实现：六个阶段由真实模型驱动、对象存储托管产物、预览窗用 iframe 加载真实地址，并补上配额退款、阶段原子抢占与陈旧回收等可靠性保障。

## Key Decisions
| Date | Decision | By | Rationale |
|------|----------|-----|-----------|
| 2026-09-22 | 阶段一不接入后端，生成流程用本地定时状态机表达 | Alex | 演示期只需表达产品逻辑，避免无需求的后端复杂度 |
| 2026-09-22 | 预览窗用真实 React 组件渲染，而非截图 | Alex | 与 mission 的「不生成点不动的假界面」一致，也更可信 |
| 2026-09-22 | 模板缩略图用 CSS 线框 | Alex | 避免无意义 AI 配图，保持工程风一致 |
| 2026-09-22 | 流水线由五步对齐为六步（补入测试验证） | Alex | 与 `docs/plan.md` 的状态模型及 mission 原则 2 对齐 |
| 2026-09-22 | 未落地模块统一使用「建设中」占位页，不伪造成已完成 | Alex | 落实 mission 原则 1 与原则 2 |
| 2026-09-22 | 阶段二前端改动面：生成链路、账号状态、项目列表与详情页、导航新增「我的项目」 | Alex | 首页既有区块与布局结构未改动，降低回归面 |
| 2026-09-22 | 账号统一走 `client.auth.*`，不启用仓库中旧的 Axios 认证工具 | Alex | 避免出现第二套认证体系 |
| 2026-09-22 | 阶段推进由服务端按任务创建时间推导，前端只轮询 | Alex | 过程可查询、刷新不丢失，且无需引入队列 |
| 2026-09-22 | 失败停在失败阶段，重试开启新一批任务且不重复扣额度 | Alex | 落实「失败不清空」，同时避免配额争议 |
| 2026-09-22 | 阶段二 `preview_url` 留空，不伪造可访问地址 | Alex | 真实部署地址属于阶段三，不制造假成功态 |
| 2026-09-22 | 「免费开始」用 sessionStorage 意图 + 全局兜底消费实现登录后回跳聚焦 | Alex | 登录落点由平台 `/auth/callback` 控制，应用无法指定回跳地址 |
| 2026-09-22 | 未登录「免费开始」指向产品侧 `/signup` 注册页，由该页调起平台账号页完成注册 | Alex | Web SDK 仅提供 `me/toLogin/login/logout` 四个认证方法，注册与登录共用平台账号页，因此用独立注册入口承接「注册」语义 |
| 2026-09-23 | 移除首页页脚与账号状态无关的硬编码「登录」入口 | Alex | 该入口不随认证状态变化，已登录时仍显示「登录」，与顶栏账号区语义冲突 |
| 2026-09-23 | 发布记录按发布节奏在 `src/data/changelog.ts` 顶部追加，最新为 `v0.7.0`；页面自动置顶并标记 LATEST | Alex | 更新日志是平台自身的发布事实，集中单一数据源避免版本号散落多处 |
| 2026-09-23 | 追加 `v0.8.0`（真实生成与可访问产物）补齐已交付但未记录的阶段三内容；`docs/changelog.md` 与数据源逐条同步 | Alex | 阶段三已落地却未进发布记录，更新日志与代码事实脱节；发布记录必须反映真实已交付能力 |
| 2026-09-23 | 阶段三预览窗由组件渲染改为 iframe，加载对象存储产物的真实可达地址 | Alex | 生成结果已是真实可运行应用，iframe 才能如实呈现，避免用本地组件冒充生成产物 |
| 2026-09-23 | 产物地址不入库，只保存对象键 `artifact_key`，预览地址每次即时解析 | Alex | 签名地址有有效期，持久化会在过期后产生失效链接 |
| 2026-09-23 | 配额扣减改为条件原子更新，模型失败时退款 | Alex | 并发首次创建不超扣，模型不可用时不白扣用户额度 |
| 2026-09-23 | AI 与对象存储调用前后拆分数据库事务，阶段用原子抢占 + `600` 秒陈旧回收 | Alex | 避免长事务占用连接池，且多端轮询不会重复执行同一阶段 |
| 2026-09-23 | 登出回跳补上 `/logout-callback` 路由，替代原先无路由的空白落点 | Alex | 平台登出会跳回该地址，缺少路由会让用户登出后停在空白页 |
| 2026-09-23 | 结构化输出解析加本地容错（尾随逗号清理 + 截断括号/字符串补全），模型修复调用作为兜底 | Alex | 模型偶发截断或尾随逗号时，不必再花一次调用，也不会因小瑕疵中断整条生成链 |
| 2026-09-23 | 模型调用失败时退还配额并保证不落半成品项目 | Alex | 外部额度或服务异常属于平台侧问题，不应由用户承担额度损失，也不该产生不可用的项目记录 |
| 2026-09-23 | 阶段执行拆为「短事务抢占 → 无连接慢调用 → 短事务回写」三段 | Alex | 整段慢调用持有连接会与并发轮询争夺连接池，在无服务器小池下直接表现为池超时并放大为网关 502 |
| 2026-09-23 | 无服务器连接池由 `1/0` 放宽为 `5/5`（可用环境变量覆盖） | Alex | 单连接会让后台阶段与浏览器轮询互相阻塞，5 秒获取超时即抛 `QueuePool limit` 并返回 5xx |
| 2026-09-23 | 请求在等待后台工作器前先归还数据库连接，快照用独立短会话重读 | Alex | 请求持有连接等待工作器时，工作器自身也需连接，形成自锁式争抢；短会话重读同时保证响应是最新状态 |
| 2026-09-23 | 后端开发文档独立成篇（`docs/backend.md`），不复用平台模板 `app/backend/README.md` | Alex | 模板 README 是平台开发规范，需保持原样；项目自身的后端实现说明另立文档，避免两者互相污染 |
| 2026-09-23 | 文档以「实际代码」为唯一事实来源，一次性统一 `mission.md`、`plan.md`、前后端 README、`.wiki.md` 与上下文文件 | Alex | 阶段三实现方式已与阶段二描述分歧（预览形态、异步执行、产物地址解析），文档歧义会误导后续迭代 |
| 2026-09-23 | 历史决策行保留原文，不再回改；阶段状态变化通过新增决策行与概览段说明 | Alex | 决策表是带日期的历史记录，回改会抹掉当时的判断依据 |
| 2026-09-23 | 后续阶段顺序调整：测试用例生成（阶段四）→ Bug 自动修复（阶段五）→ 对话式增量修改与版本回溯（阶段六，原阶段四后移） | Alex | 先把生成结果的测试与缺陷修复闭环做扎实，再基于稳定产物做多轮对话式迭代；顺序变化同步至 `docs/plan.md`、`docs/frontend.md`、`app/frontend/README.md`、`docs/mission.md` 与上下文文件 |
| 2026-09-23 | 测试用例与运行记录独立建表（`test_cases`、`test_runs`），不复用 `build_tasks` / `test_report_json` | Alex | 阶段四的测试是对已交付产物的**可重复复测**，与生成过程中的一次性内联检查语义不同；独立表才能支撑用例 CRUD 与多次运行历史对比 |
| 2026-09-23 | 用例通过与否由产物内容做确定性断言匹配决定，不由模型判定 | Alex | 让模型自评测试结果等于伪造通过态；执行结果必须可复现、可解释，才能支撑后续 Bug 自动修复阶段 |
| 2026-09-23 | 无 `artifact_key` 的项目拒绝生成用例与执行，返回可见错误 | Alex | 没有产物就没有可测对象，伪造用例或空跑通过的绿标会误导用户 |
| 2026-09-23 | 重复生成用例只替换 `source=auto` 的用例，保留用户手动新增的用例 | Alex | 自动生成是辅助手段，用户手工补充的用例属于用户资产，不应被一次重新生成清空 |
| 2026-09-23 | `case_state` 控制启用状态而非物理删除；删除用例时保留历史运行记录 | Alex | 停用比删除更可逆；历史运行是已经发生的事实，不应因用例被删而丢失，否则测试历史失去可追溯性 |
| 2026-09-23 | 发布记录追加 `v0.9.0`（测试用例与执行闭环），累计 9 个版本、53 条；`docs/changelog.md` 与数据源逐条同步 | Alex | 阶段四已交付但未进发布记录，更新日志会与代码事实脱节；发布记录必须与真实能力一致 |
| 2026-09-23 | 文档层面把「测试层」列为已落地能力，并把测试用例管理从待落地清单移出 | Alex | 阶段四已闭环（生成、执行、历史、隔离、清理），继续标记为未落地会让文档与实现相互矛盾 |
| 2026-09-23 | 缺陷与修复记录独立建表（`bugs`、`bug_fix_logs`），不复用测试表 | Alex | 缺陷是「已确认的问题」，修复日志是「针对它的多次尝试」，与用例/运行的语义不同；独立表才能承载尝试序号、源/目标版本与逐次复测结论 |
| 2026-09-23 | 自动修复产出新版本产物并前移项目指针，旧版本对象保留 | Alex | 修复结果必须可回看、可对比；覆盖式改写会让用户无法判断修的是哪一版，也让历史修复记录失去对应产物 |
| 2026-09-23 | 修复是否成功由「新产物上的复测结论」决定，模型自述不作为判定依据 | Alex | 与阶段四同一口径：模型说修好了不算修好，只有在真实产物上重跑用例通过才算修复成功 |
| 2026-09-23 | 修复失败如实落一条失败记录并把缺陷置为修复失败，不回退项目版本与产物指针 | Alex | 失败是真实事实，必须可见可重试；回退指针会让「已修复到 v2 后再失败一次」把可用产物一起丢掉 |
| 2026-09-23 | 修复期间缺陷置为「修复中」并拒绝并发修复，且不允许被手动状态覆盖 | Alex | 同一条缺陷并发触发会产出互相覆盖的版本与重复扣费式调用；状态锁是防止重复修复的最小手段 |
| 2026-09-23 | 前端自动修复请求不启用自动重试，成功后同时刷新缺陷、测试与流水线三处缓存 | Alex | 修复会改写产物且消耗模型调用，自动重试会重复触发；新版本同时影响测试面板与项目版本展示，不刷新会让页面与事实脱节 |
| 2026-09-23 | 产物删除能力下沉到 `generation_artifacts.delete_object()`，验证脚本在 `finally` 中回收自建对象 | Alex | 此前删除项目只清业务表，验证与删除都会在对象存储留下孤儿对象；回收口径需要单一入口，避免每个调用方各自拼存储参数 |
| 2026-09-23 | 发布记录追加 `v0.10.0`（缺陷提交与自动修复），累计 10 个版本、65 条；`docs/changelog.md` 与数据源逐条同步，并清除文档内残留的旧版本号与旧条目数 | Alex | 阶段五已交付但发布记录与文档仍停在阶段四口径，会让更新日志与代码事实脱节；文档必须与已验证的实现一致 |

## Constraints

- 视觉规范以 `app/frontend/DESIGN.md` 为准：暗色为唯一主题、主色 `#C8F751` 占比 ≤10%、不使用蓝紫渐变。
- 更新日志为平台自身的发布记录（`/changelog`），与用户生成项目的变更追溯是两个不同对象，后者未落地。
- 产品边界以 `docs/mission.md` 为准：不做通用代码补全、不做拖拽搭建器、不做假界面。
- 前端固定为 shadcn/ui + Vite 技术栈；后端后续使用 Atoms Cloud，不自建第二套认证。
- 阶段二实际改动面为：生成链路（`run()` + 轮询）、账号状态、项目列表与详情页、导航新增「我的项目」；首页既有区块与布局结构未被改动。
- 构建策略：开发任务收尾时运行 `pnpm run lint && pnpm run build` 作为验证；纯文档或问答轮次不做构建。
- 交付策略：收到明确的开发或文档更新指令后动手；未获指令时只回答与分析，不主动重构。
